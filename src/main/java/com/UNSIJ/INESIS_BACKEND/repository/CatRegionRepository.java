package com.UNSIJ.INESIS_BACKEND.repository;

import com.UNSIJ.INESIS_BACKEND.model.CatRegion;
import org.springframework.data.jpa.repository.JpaRepository;

public interface CatRegionRepository extends JpaRepository<CatRegion, Long> {
}
